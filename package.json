{
    "name": "time-timer-electron",
    "version": "1.0.0",
    "main": "main.js",
    "scripts": {
      "start": "electron .",
      "package": "electron-packager . time-timer-electron --overwrite --platform=darwin --arch=x64 --icon=icon.icns --prune=true --out=release-builds"
    },
    "devDependencies": {
      "electron": "^27.0.0",
      "electron-packager": "^17.1.1"
    }
  }